import { Link } from 'react-router-dom';

const SearchBar = () => {
  return (
    <Link to="/search">
      <div className='search-bar-container'></div>
    </Link>
  )
};

export default SearchBar;
